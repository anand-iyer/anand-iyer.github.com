<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[]]></title>
  <link href="http://anand-iyer.com/atom.xml" rel="self"/>
  <link href="http://anand-iyer.com/"/>
  <updated>2014-06-01T01:28:45-04:00</updated>
  <id>http://anand-iyer.com/</id>
  <author>
    <name><![CDATA[Anand]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Fixing missing bootcamp partition in OSX]]></title>
    <link href="http://anand-iyer.com/blog/2014/fixing-missing-bootcamp-partition-in-osx/"/>
    <updated>2014-05-28T04:25:00-04:00</updated>
    <id>http://anand-iyer.com/blog/2014/fixing-missing-bootcamp-partition-in-osx</id>
    <content type="html"><![CDATA[<p><strong>Symptom:</strong> You use Bootcamp to dual-boot OSX and Windows. After installing Windows, you are not able to see the BOOTCAMP partition in OSX. If you try viewing the disk using disk utility, the BOOTCAMP partition is greyed out, and shows up as FAT formatted although it is NTFS formatted.</p>

<p>Turns out the issue occurs because of a modification during Windows installation. Once Bootcamp Assistant (BCA) created the BOOTCAMP partition and restarted the system, I noticed that there was an additional small-sized partition created (~600MB) just before the BOOTCAMP partition. Instead of ignoring it, I deleted and recreated the bootcamp partition from Windows setup hoping the wasted space would be reclaimed. That was a bad idea.</p>

<p>When you do this, it confuses the hybrid GPT/MBR partitioning scheme used by BCA. Essentially, when you remove/resize/add partitions via Windows setup, that information gets updated in the MBR record, but is not synced to the GPT record. Thus, Windows will see the OSX partition, but not vice-versa.</p>

<p>The easy way to fix this without going through the pain of removing and reinstalling Windows from scratch is to sync the MBR info back to the GPT record. Here&rsquo;s how to do it:</p>

<p>First, we need detailed information on the MBR record&mdash;-where the partition starts, where it ends etc. For this purpose, use the <code>gptsync</code> tool. Homebrew has the gptsync package, but if you try to use it, you&rsquo;ll end up with the following error:</p>

<pre><code>Warning: Unknown GPT spec revision 0x00010000 
</code></pre>

<p>This is because of 64-bit incompatability. A fixed version of gptsync aimed at OSX, extracted from rEFIt is available <a href="https://github.com/JrCs/gptsync">here</a>. Just compile it using Xcode, and you&rsquo;re good to go.</p>

<p>Running <code>gptsync</code> on my machine resulted in the following:</p>

<pre><code>&gt; ./gptsync /dev/disk0

Current GPT partition table:
 #      Start LBA      End LBA  Type
 1             40       409639  EFI System (FAT)
 2         409640    250801735  Mac OS X HFS+
 3      250801736    252071271  Mac OS X Boot
 4      252071936    500117503  Basic Data

Current MBR partition table:
 # A    Start LBA      End LBA  Type
 1              1       409639  ee  EFI Protective
 2         409640    250801735  af  Mac OS X HFS+
 3 *    250802176    251518975  07  NTFS/HPFS
 4      251518976    500115455  07  NTFS/HPFS   
</code></pre>

<p>In the scenario above, the solution was to modify the GPT table to match the MBR table. Essentially, delete partitions 3 and 4, and then recreate it as per MBR. For this purpose, I used the gdisk tool available <a href="http://www.rodsbooks.com/gdisk/">here</a>. Once installed, run the tool (usage instructions are similar to fdisk, tutorial is available on the tool&rsquo;s website). I used <code>d</code> command to delete partitions 3 and 4, <code>n</code> command to recreate them with first sector and last sector noted from MBR and type 0700. Once done, check the changes using command <code>p</code>, and verify for errors using <code>v</code>. If there are no errors, confirm the changes by writing them with command <code>w</code>, reboot and enjoy!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Re-signing IPA files]]></title>
    <link href="http://anand-iyer.com/blog/2013/re-signing-ipa-files/"/>
    <updated>2013-03-28T23:49:00-04:00</updated>
    <id>http://anand-iyer.com/blog/2013/re-signing-ipa-files</id>
    <content type="html"><![CDATA[<p>Scenario: You have an IPA that you want to test on a device, but the IPA was generated using a provisioning profile that does not include the device, and was signed with a certificate that is not yours. You drag this IPA to XCode&rsquo;s organizer window hoping it would be installed on the connected device, but this results in an <code>Unknown error</code>.</p>

<p>Turns out that this is easy to fix. Here&rsquo;s how to make it work.</p>

<p>First, unpack the IPA (<code>unzip -q &lt;IPA&gt;</code>). This would create a directory named <code>Payload</code> and a few other files. Delete the existing signature in the extracted package:</p>

<pre><code>rm -rf "Payload/*.app/_CodeSignature" "Payload/*.app/CodeResources"
</code></pre>

<p>For the next step, you need two things: a valid provisioning profile and a valid certificate. Both can be obtained via the iOS developer portal.</p>

<p>Replace the provisioning profile first</p>

<pre><code>cp &lt;PROVISIONING_PROFILE&gt; "Payload/*.app/embedded.mobileprovision"
</code></pre>

<p>Then resign using your certificate (the certificate must be present in your keychain)</p>

<pre><code>/usr/bin/codesign -f -s "&lt;CERTIFICATE&gt;" --resource-rules "Payload/*.app/ResourceRules.plist" "Payload/*.app"
</code></pre>

<p>Now you can recreate the IPA and then drag &amp; drop it in the XCode organizer to install it on the device!</p>

<pre><code>zip -qr &lt;IPA_resigned&gt; Payload     
</code></pre>

<p><strong>Edit:</strong> If you are looking for user friendly options, check out the <a href="http://floatlearning.com/2011/11/re-signing-an-ios-app-without-xcode/" target="_blank">script by Daniel</a> or the <a href="http://www.gorbster.net/blog/archives/273" target="_blank">app by Brian</a>.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Gnuplot cairo terminal errors in Mountain Lion]]></title>
    <link href="http://anand-iyer.com/blog/2013/gnuplot-cairo-terminal-errors-in-mountain-lion/"/>
    <updated>2013-03-17T20:31:00-04:00</updated>
    <id>http://anand-iyer.com/blog/2013/gnuplot-cairo-terminal-errors-in-mountain-lion</id>
    <content type="html"><![CDATA[<p>I love the cairo terminal in gnuplot, it creates amazing plots. My recent upgrade to Mountain Lion broke my homebrew based gnuplot installation, throwing a bunch of glib errors when I set the terminal to pdfcairo or pngcairo (normal terminals work fine):</p>

<pre><code>Error: GLib-GObject-CRITICAL **: gtype.c:2720: You forgot to call g_type_init() 
</code></pre>

<p>This is essentially an issue with the latest version of Pango package. There are two ways to fix this:</p>

<ul>
<li>Force homebrew to go back to a lower version of Pango; this error seems to appear in versions 1.32.6 or later.</li>
<li>Patch the gnuplot installation&rsquo;s Cairo terminal as suggested <a href='https://github.com/mxcl/homebrew/pull/18528' target='_blank'>here</a>.</li>
</ul>


<p><strong>Note:</strong> If you are not getting cairo terminals in your gnuplot installation, reinstall it with cairo option enabled: <code>brew install gnuplot --cairo</code></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Latex Tips Collection]]></title>
    <link href="http://anand-iyer.com/blog/2013/latex-tips-collection/"/>
    <updated>2013-01-13T10:56:30-05:00</updated>
    <id>http://anand-iyer.com/blog/2013/latex-tips-collection</id>
    <content type="html"><![CDATA[<p>Over time, I&rsquo;ve come across many useful Latex tips and tricks scattered across the internet. Here&rsquo;s an attempt to collect some of those:</p>

<ul>
<li><p><strong><a href="http://gurmeet.net/computer-science/latex-tips-n-tricks-for-conference-papers/">Tips for Conference Papers</a> by Gurmeet Singh</strong></p>

<p>A nice set of tricks that saves your life at the last minute during deadlines.</p></li>
<li><p><strong><a href="http://www.read.seas.harvard.edu/~kohler/latex.html">Latex Usage Notes</a> by Eddie Kohler</strong></p>

<p>Solutions for common Latex problems, tips on proper formatting, etc.</p></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Syncing folders outside Dropbox folder]]></title>
    <link href="http://anand-iyer.com/blog/2012/syncing-folders-outside-dropbox-folder/"/>
    <updated>2012-10-16T02:25:03-04:00</updated>
    <id>http://anand-iyer.com/blog/2012/syncing-folders-outside-dropbox-folder</id>
    <content type="html"><![CDATA[<p>A big complaint I have about <a href="http://db.tt/VZd1Zsgp">Dropbox</a> is that unlike <a href="https://www.sugarsync.com/referral?rf=fevam6f0oyxab&amp;utm_source=website&amp;utm_medium=web&amp;utm_campaign=referral&amp;shareEvent=2630716">SugarSync</a>, it does allow me to sync any folder in my computer (essentially, I need to place the folders I want to be synced inside the &ldquo;Dropbox&rdquo; folder).</p>

<p>Recently, I found a workaround for this problem. Here&rsquo;s the trick to syncing any folder in your computer: softlink the folder to your Dropbox folder!</p>

<p>If you&rsquo;re using a Mac (or Linux), just use the following command:</p>

<pre><code>ln -s /full-path-to-folder/file-to-sync ~/Dropbox/desired-folder-name
</code></pre>

<p>That&rsquo;s it, from now on, all file(s) in the actual path would now sync with Dropbox.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Doxygen and OSX: Graphviz and other errors]]></title>
    <link href="http://anand-iyer.com/blog/2011/doxygen-and-osx/"/>
    <updated>2011-10-09T19:16:05-04:00</updated>
    <id>http://anand-iyer.com/blog/2011/doxygen-and-osx</id>
    <content type="html"><![CDATA[<p>I&rsquo;m a big fan of <a href="http://www.doxygen.org/">Doxygen</a>. I use it all the time to generate and analyze call trees. Recently, I wanted to use it in my MacbookPro for analyzing an open source project. There is a binary for SnowLeopard <a href="http://www.stack.nl/~dimitri/doxygen/download.html">here</a> that works in Lion too, but it was throwing lots of errors regarding Graphviz. Here are two things that I had to do to make it finally work:</p>

<ul>
<li><p>For some reason, long file names do not work in OSX. By default, Doxygen uses long names. So you need to set the <code>SHORT_NAMES</code> to <code>YES</code> in your doxygen file. If you are using the Wizard (GUI), then save your settings file (<code>File-&gt;Save</code>), and then manually edit the file in a text editor. </p></li>
<li><p>GraphViz is included in the dmg file. You need to set appropriate path to the dot file for graphviz to work. Try setting it to <code>/Applications/Doxygen.app/Contents/Resources</code>. If that fails, then use macports to install graphviz, and use that path.</p></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Making QXDM COM object work with Python]]></title>
    <link href="http://anand-iyer.com/blog/2011/making-qxdm-com-object-work-with-python/"/>
    <updated>2011-02-19T11:21:17-05:00</updated>
    <id>http://anand-iyer.com/blog/2011/making-qxdm-com-object-work-with-python</id>
    <content type="html"><![CDATA[<p>Qualcomm&rsquo;s <a href="http://www.qualcomm.com/products_services/testing_diagnostics/qxdm_pro.html">QXDM</a> is an amazing tool for getting diagnostic information from mobile phones. It supports automation where you can write scripts to interface with the COM object exposed by QXDM.</p>

<p>The default installation of QXDM comes with a number of examples on how to use this object. Unfortunately, all the examples are in Perl. Although I like Perl, recently I have been using Python for all my scripting needs. It&rsquo;s actually very easy to convert all these scripts to Python. Here&rsquo;s how:</p>

<ul>
<li><p>First, you need Python to be able to access COM objects. For this purpose, I used the Python comtypes package. Download and install it from <a href="http://sourceforge.net/projects/comtypes/files/">SourceForge</a>.</p></li>
<li><p>Now, you can do the following in your script:</p></li>
</ul>


<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
</pre></td><td class='code'><pre><code class='python'><span class='line'><span class="kn">from</span> <span class="nn">comtypes.client</span> <span class="kn">import</span> <span class="n">CreateObject</span>
</span><span class='line'>
</span><span class='line'><span class="c"># For ISF file post-processing</span>
</span><span class='line'><span class="n">IISF</span> <span class="o">=</span> <span class="n">CreateObject</span><span class="p">(</span><span class="s">&quot;DMCoreAutomation.ItemStoreFiles&quot;</span><span class="p">)</span>
</span><span class='line'>
</span><span class='line'><span class="c"># For accessing data directly from QXDM. </span>
</span><span class='line'><span class="c"># Note that the script needs to be run as admin.</span>
</span><span class='line'><span class="n">QXDM</span> <span class="o">=</span> <span class="n">CreateObject</span><span class="p">(</span><span class="s">&quot;QXDM.Application&quot;</span><span class="p">)</span><span class="err"> </span>
</span></code></pre></td></tr></table></div></figure>



]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[It's raining job offers at CET!]]></title>
    <link href="http://anand-iyer.com/blog/2010/its-raining-job-offers-at-cet/"/>
    <updated>2010-12-24T06:38:31-05:00</updated>
    <id>http://anand-iyer.com/blog/2010/its-raining-job-offers-at-cet</id>
    <content type="html"><![CDATA[<p>Seems like it has been a fantastic placement season for the 2011 batch at my alma mater, <a href="http://en.wikipedia.org/wiki/College_of_Engineering,_Trivandrum">College of Engineering, Trivandrum</a> (CET). According to <a href="http://www.hinduonnet.com/2010/12/21/stories/2010122165900300.htm">this</a> article from <strong>The Hindu</strong>, 1019 job offers were made to a total of 540 students. Here&rsquo;s the breakdown (courtesy: The Hindu):</p>

<p><img src="http://www.anand-iyer.com/blog/wp-content/uploads/2010/12/2010122165900301.jpg" alt="" /></p>

<p>Way to go CETians!!</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Qualnet 4.0.1 woes!]]></title>
    <link href="http://anand-iyer.com/blog/2010/qualnet-4-0-1-woes/"/>
    <updated>2010-12-23T04:06:15-05:00</updated>
    <id>http://anand-iyer.com/blog/2010/qualnet-4-0-1-woes</id>
    <content type="html"><![CDATA[<p>For my latest work, I am using the Qualnet simulator tool. Unfortunately, the version that I have access to is pretty old (version 4.0.1). Consequently, I had a lot of trouble getting it to compile in Windows 7. For those who are having the same trouble, see if what I did works for you:</p>

<ul>
<li><p>This version of Qualnet is only compatible with Visual Studio 2005 (I have tried VS 2008 and VS 2010 &ndash; although I succeeded in compiling, the executable crashes all the time). Install VS 2005 if you can get hold of it, or install Visual C++ 2005 Express edition which is available for free.</p></li>
<li><p>The second thing that is essential for building Qualnet is Windows 7 SDK. Download and install this from Microsoft.</p></li>
<li><p>Once you have both these installed, run the <code>vsvars32.bat</code> file to set the right environment variables. <code>vsvars32.bat</code> is available in the following folder: <code>C:\Program Files\Microsoft Visual Studio 8\Common7\Tools\</code></p></li>
<li><p>Now try compiling qualnet. I used the following command inside <code>c:\qualnet\4.0\main</code> folder:</p>

<pre><code>nmake /f Makefile-windows-vc8
</code></pre></li>
<li><p>For most, the compilation will fail giving errors in <code>oslrv2_nigata.cpp</code>. So go ahead and comment out this file in the makefile present in <code>qualnet\libraries\wireless\src</code></p></li>
<li><p>Try compiling again. Now the compiler would throw errors wherever routines from the file that we commented out. I had to comment out its usage wherever compilation broke.</p></li>
<li><p>Once I did that, compilation went smooth, and I was able to get qualnet working.</p></li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[ImportError with IronPython in C#]]></title>
    <link href="http://anand-iyer.com/blog/2010/importerror-with-ironpython-in-c/"/>
    <updated>2010-09-30T18:48:44-04:00</updated>
    <id>http://anand-iyer.com/blog/2010/importerror-with-ironpython-in-c</id>
    <content type="html"><![CDATA[<p>I was using IronPython to execute python code inside my C# implementation lately, and I encountered this error when trying to use xmlrpclib:</p>

<pre><code>ImportError: No module named xmlrpclib.
</code></pre>

<p>It was really frustrating because if I try the same in IronPython console, it works fine. It turned out that this was a problem with search paths. When called via C# code, IronPython does not search for missing libraries unless you provide it a path to search for. Here&rsquo;s how I solved this issue:</p>

<ol>
<li>First, find out the search paths that IronPython is using in console mode:</li>
</ol>


<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class='python'><span class='line'><span class="kn">import</span> <span class="nn">sys</span>
</span><span class='line'><span class="k">print</span> <span class="n">sys</span><span class="o">.</span><span class="n">path</span>
</span></code></pre></td></tr></table></div></figure>


<p>This will print all the search paths. Save these paths somewhere.</p>

<ol>
<li>Now include all these search paths inside your C# code:</li>
</ol>


<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
</pre></td><td class='code'><pre><code class='csharp'><span class='line'><span class="n">ScriptEngine</span> <span class="n">Engine</span> <span class="p">=</span> <span class="n">Python</span><span class="p">.</span><span class="n">CreateEngine</span><span class="p">();</span>
</span><span class='line'><span class="n">ICollection</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Paths</span> <span class="p">=</span> <span class="n">Engine</span><span class="p">.</span><span class="n">GetSearchPaths</span><span class="p">();</span>
</span><span class='line'><span class="n">Paths</span><span class="p">.</span><span class="n">Add</span><span class="p">(</span><span class="s">&quot;&lt;Path&gt;&quot;</span><span class="p">);</span>
</span><span class='line'><span class="n">Engine</span><span class="p">.</span><span class="n">SetSearchPaths</span><span class="p">(</span><span class="n">Paths</span><span class="p">);</span>
</span></code></pre></td></tr></table></div></figure>


<p>Replace <code>&lt;Path&gt;</code> with the path you saved earlier. Now IronPython will search all these pathsbefore failing with an ImportError.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Conky like system monitor for Windows]]></title>
    <link href="http://anand-iyer.com/blog/2010/conky-like-system-monitor-for-windows/"/>
    <updated>2010-06-23T09:15:57-04:00</updated>
    <id>http://anand-iyer.com/blog/2010/conky-like-system-monitor-for-windows</id>
    <content type="html"><![CDATA[<p>Ever since I came across <a href="http://conky.sourceforge.net/">Conky</a>, I have been using it on all my machines running Ubuntu (for those who are interested, I use <a href="http://gnome-look.org/content/show.php/CONKY-colors?content=92328">Conky-Colors</a> for easy configuration). I also use Windows extensively and had always wanted to have something similar to Conky. It seems like my quest has finally ended. My good friend suggested that I try <a href="http://rainmeter.net/">RainMeter</a>, and I was impressed with the results! It&rsquo;s extremely customizable, has tons of skins to choose from, and is not heavy on the system.</p>

<p>Here&rsquo;s a screenshot (the theme I&rsquo;m using is <a href="http://jsmorley.deviantart.com/art/JSMeterII-161282085">JSMeterII</a>):</p>

<p><img src="http://anand-iyer.com/images/Desktop-300x187.png" alt="" /></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[NS3 in Eclipse]]></title>
    <link href="http://anand-iyer.com/blog/2010/ns3-in-eclipse/"/>
    <updated>2010-05-30T14:58:55-04:00</updated>
    <id>http://anand-iyer.com/blog/2010/ns3-in-eclipse</id>
    <content type="html"><![CDATA[<p>I found <a href="http://www.nsnam.org/wiki/index.php/HOWTO_configure_eclipse_with_ns-3">this</a> wiki page on configuring NS3 with Eclipse and decided to give it a try. I had trouble compiling, and Eclipse kept giving the following error:</p>

<pre><code>Cannot run program "waf": Unknown reason)
</code></pre>

<p>After spending a little time with the configuration, the following additional tweaking seems to be working (in Ubuntu 10.04, Eclipse version 3.5.2 Galileo with ns-allinone-3.8):</p>

<ol>
<li><p>Create a new External tool configuration. The location should be the location to waf (<code>${workspace_loc:/ns3/waf}</code> in my case) and working directory should be the directory where waf is present (<code>${workspace_loc:/ns3/}</code> in my case). In arguments textbox, provide the following:</p>

<pre><code> --run "${string_prompt}"
</code></pre>

<p>Now when you try to build the project (right click on the <code>project -&gt; Build Project</code>), it should work.</p></li>
<li><p>To run NS3 programs using waf, you need to create a new Run configuration. Make the new configuration to be a C/C++ application. Select the project you want this run configuration to be associated with, and give the complete path to waf as the C/C++ program. Click on the arguments tab, and provide the same argument as above. Now when you try to run a program, a dialog box should pop-up asking for the input arguments to waf and your program should run.</p></li>
</ol>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[NS2 in Ubuntu 10.04]]></title>
    <link href="http://anand-iyer.com/blog/2010/ns2-in-ubuntu-10-04/"/>
    <updated>2010-05-19T07:13:49-04:00</updated>
    <id>http://anand-iyer.com/blog/2010/ns2-in-ubuntu-10-04</id>
    <content type="html"><![CDATA[<p>I spent a reasonably long time getting NS-2 (ns-allinone-2.34) to work in Ubuntu 10.04. Here&rsquo;s how I finally did it:</p>

<p>Install necessary tools for building:</p>

<pre><code>sudo apt-get install build-essential autoconf automake libxmu-dev gcc-4.3
</code></pre>

<p>It turns out that otcl does not like the version of gcc installed by default in Lucid. So go ahead and change <code>ns-allinone-2.34/otcl-1.13/Makefile.in</code>. Find the line that says:</p>

<pre><code>CC=    @CC@
</code></pre>

<p>and change it to:</p>

<pre><code>CC=    gcc-4.3
</code></pre>

<p>Now try doing <code>./install</code> from the ns-allinone-2.34 directory. It should work.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[ath5k “Master” mode]]></title>
    <link href="http://anand-iyer.com/blog/2010/ath5k-master-mode/"/>
    <updated>2010-05-06T12:11:34-04:00</updated>
    <id>http://anand-iyer.com/blog/2010/ath5k-master-mode</id>
    <content type="html"><![CDATA[<p>To enable Master mode in ath5k, do the following:</p>

<ol>
<li><p>Make sure you are running kernel 2.6.29 or higher. If not, you need to get the latest patches for ath5k.</p></li>
<li><p>Install hostapd. It is available in Ubuntu repositories.</p></li>
<li><p>Create a config file for hostapd. A simple one is:</p>

<pre><code> interface=wlan0
 driver=nl80211
 ssid=test
 channel=1
</code></pre></li>
<li><p>Now you can start hostapd to put the card in master mode:</p>

<pre><code> sudo hostapd &lt;config_file&gt;
</code></pre></li>
</ol>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Royaltek RBT-2300 and Karmic Koala]]></title>
    <link href="http://anand-iyer.com/blog/2010/royaltek-rbt-2300-and-karmic-koala/"/>
    <updated>2010-03-31T18:48:46-04:00</updated>
    <id>http://anand-iyer.com/blog/2010/royaltek-rbt-2300-and-karmic-koala</id>
    <content type="html"><![CDATA[<p>I recently had to make use of a GPS logger in Linux. Here&rsquo;s how I made it work in Karmic, incase any one is interested:</p>

<ul>
<li><p>First, make sure you have necessary bluetooth packages. If not, install it using:</p>

<pre><code>  sudo apt-get install bluetooth
  sudo apt-get install libbluetooth-dev
</code></pre></li>
<li><p>Switch on the GPS, scan and check if you can find the device:</p>

<pre><code>  sudo hcitool scan
</code></pre></li>
</ul>


<p>The output would be something like the following:</p>

<pre><code>Scanning ...
 00:aa:bb:cc:dd:ee    BlueGPS CCDDEE
</code></pre>

<p>We are interested in the channel in which the device operates. Let&rsquo;s find that out:</p>

<pre><code>sudo sdptool records 00:aa:bb:cc:dd:ee
</code></pre>

<p>This will give you lots of information about the device. Pay attention to the channel. For instance, my device says:</p>

<pre><code>Service Name: Dev B
Service RecHandle: 0x10000
Service Class ID List:
 "Serial Port" (0x1101)
Protocol Descriptor List:
 "L2CAP" (0x0100)
 "RFCOMM" (0x0003)
 &lt;strong&gt;Channel: 1&lt;/strong&gt;
Language Base Attr List:
 code_ISO639: 0x656e
 encoding:    0x6a
 base_offset: 0x100
</code></pre>

<ul>
<li><p>Use this channel to create an entry in the <code>rfcomm.conf</code> file:</p>

<pre><code>  sudo gedit /etc/bluetooth/rfcomm.conf
</code></pre></li>
</ul>


<p>enter the following at the end of the file:</p>

<pre><code># Bluetooth GPS
rfcomm2 {
    bind yes,
    device 00:aa:bb:cc:dd:ee,
    channel 1,
    comment "Serial Port",
}
</code></pre>

<p>Once you have done this, you can connect to the device:</p>

<pre><code>$ sudo rfcomm connect 2
Connected /dev/rfcomm2 to 00:aa:bb:cc:dd:ee on channel 1
Press CTRL-C for hangup
</code></pre>

<ul>
<li><p>I found rtkgps (<a href="http://wohlberg.net/public/software/misc/rtkgps/">http://wohlberg.net/public/software/misc/rtkgps/</a>) to be a good utility to access GPS logs.</p>

<pre><code>  rtkgps -d /dev/rfcomm2 read
</code></pre></li>
</ul>

]]></content>
  </entry>
  
</feed>
